# EagleBridge 0.3.5 更新说明与视频提纲

这次更新让 EagleBridge 从笔记里的素材连接工具，扩展到白板、多素材库、引用整理和附件迁移。图片、视频等素材可以交给 Eagle 管理，同时在 Obsidian 里保留预览、引用和编辑入口。

整理范围：以 `749eeaeb759279761e3c5fe0931ce8f8a80b1f1c` 为旧版本基线，汇总到 `201b5213f2a13b75931a40195acb604a8f7b77f1` 的 24 次提交，并补充本次发布前完成的 Eagle 面板主题适配和多 Obsidian 库端口共享。以下按最终用户能使用的能力组织，不逐条复述提交。

## 1. Canvas 白板也能使用 Eagle 素材

- 支持把 Eagle 素材拖入或粘贴到 Obsidian Canvas，生成白板卡片。
- 外部素材也可以先进入 Eagle，再在白板中生成对应引用。
- 图片适配卡片尺寸；PDF、视频、音频和网页根据类型显示对应内容，实际播放能力取决于 Obsidian 支持的格式。
- 新卡片尽量放在当前可见区域，改善白板缩放、平移后找不到新卡片的问题。
- 白板中的 Eagle 引用也纳入引用关系视图和附件删除操作。

演示建议：从 Eagle 拖入一张图片和一个视频，再展示白板里的卡片及它们的引用记录。

## 2. 同时连接多个 Eagle 素材库

- 最多配置 5 个 Eagle 素材库，为每个库设置别名、端口、路径和上传文件夹 ID。
- 一篇笔记可以引用来自不同 Eagle 库的素材，不必把所有素材集中到同一个库。
- 同一个 Eagle 库可以填写多个设备路径，插件使用当前设备上第一个可访问的路径，方便 Windows 与 Mac 共用笔记。
- 不同的 Eagle 库应分别建立配置；“同库的多个设备路径”和“多个独立素材库”是两个不同用途。
- 从已配置的 Eagle 库拖入素材时直接建立引用，不会再复制一份到默认上传库。

## 3. 上传方式可以按场景选择

- 可以固定上传到默认 Eagle 库，也可以每次选择目标库。
- 在 Markdown 的上传选择中，还可以选择 Obsidian 默认嵌入，将这次素材留在笔记库里。
- 可以分别控制 Markdown 和 Canvas 的上传，并按图片、视频、网页及其他文件开关上传；网页上传默认关闭。
- 关闭对应上传后，外部附件可以继续走 Obsidian 原有处理方式。
- 各 Eagle 库可以设置自己的上传文件夹，方便把工作素材、个人收藏等分开管理。

## 4. 新增 Eagle 引用关系视图

- 查看当前 Markdown 或 Canvas 使用了哪些 Eagle 素材。
- 搜索当前 Obsidian 库已经引用的素材，支持素材名、素材 ID、笔记名、路径和库别名等信息。
- 查看一个素材被哪些文档引用、引用了多少次，并直接打开对应文档。
- 显示素材所属的 Eagle 库，提供在 Eagle、Obsidian 或系统应用中打开的入口。
- 在详情里查看素材名称，查看或修改标签、注释、来源网址。
- 界面整理为素材列表与详情，支持窄侧栏和较宽工作区，减少重复信息。

这里的搜索范围是当前 Obsidian 库中的引用记录，不是搜索 Eagle 库里所有尚未被引用的素材。

## 5. 删除附件时可以选择清理范围

- 删除前显示素材的引用文件数和出现次数。
- 可以只移除当前链接，保留 Eagle 素材。
- 可以删除 Eagle 素材并清理当前链接，或清理当前 Obsidian 库内索引到的全部相关链接。
- 支持清理 Markdown 引用和 Canvas 对应节点，素材删除使用 Eagle 回收站。

删除 Eagle 素材会影响它的其他使用位置。引用统计只覆盖当前 Obsidian 库，不包含同时打开的其他库，也不代表所有外部引用。

## 6. 当前文档附件可以批量迁移到 Eagle

- 新增 **Upload current Markdown attachments to Eagle** 命令，可自行绑定快捷键。
- 处理当前文档正文里的本地图片、视频及其他附件，同一个源附件只上传一次。
- 上传成功后，将正文引用替换为 Eagle 链接；原先是嵌入的仍保留嵌入形式，普通链接仍保留为链接。
- 大视频通过本地文件路径逐个交给 Eagle 导入，不把整个视频读入 Obsidian 的 JavaScript 内存。
- 部分文件失败时仍保留成功项，显示处理进度，避免重复执行同一任务。
- 核对 Eagle 副本和剩余引用后，才将原附件移入回收站；仍被其他 Markdown、YAML 属性或 Canvas 引用，以及校验不确定、处理中源文件变化等情况，会保留源文件并说明原因。

适合把自动采集进笔记的本地视频整理进 Eagle。目前是“当前文档正文附件”的迁移，不是跨多个文档的一键迁移；不会替换 YAML 属性中的附件路径。第三方卡片视图能否显示视频缩略图，仍取决于对应插件。

## 7. 笔记和 Eagle 附件可以一起导出

- 在 Markdown 文件菜单选择 **Export Markdown with Eagle attachments**。
- 支持导出为文件夹或 ZIP，将笔记和引用到的 Eagle 原文件一起打包。
- 导出副本中的 Eagle 链接改为相对路径，方便分享和离开本机环境后阅读；源笔记保持原样。
- 同一素材重复引用只导出一份，重名附件自动区分；Eagle 网页书签转换为原始网址。
- 找不到的素材保留原链接并提示，不会假装已经完整导出。

目前入口面向 Markdown 文件，不是整个 Obsidian 库或 Canvas 的完整打包工具。仍需检查导出报告，确认所需素材均已包含。

## 8. 标签同步方向更灵活

- 可选择关闭自动同步、把笔记的 YAML 标签追加到 Eagle，或把 Eagle 素材标签合并到笔记 YAML。
- 笔记标签传到 Eagle 时，还可以开启覆盖模式，让 Eagle 标签与笔记标签一致。
- 保留手动同步当前页标签的命令，便于按需处理。
- 跨 Eagle 库引用按各素材所属库处理，避免更新到错误的素材库。

这些是可选的同步方向，并不是同时双向覆盖；覆盖模式会替换 Eagle 已有标签。

## 9. 从 Eagle 回到 Obsidian 更方便

- 可以手动把当前页链接发送给它引用的 Eagle 素材，也可以开启新增引用时自动发送。
- 一个素材可以保存多个笔记入口；重复发送同一入口会更新名称，避免反复堆积。
- 新增配套的 Eagle 端插件 `eagle_to_ob`，在素材检查器里展示链接按钮，支持打开、新增、修改和删除入口；Obsidian、Zotero 链接有对应图标。
- 相比早期依赖旧回跳入口的方式，笔记入口直接保存在素材信息中，便于从 Eagle 找回使用该素材的笔记。

自动生成的回跳链接使用 Advanced URI，需要安装对应 Obsidian 插件、配置 Obsidian 库标识，并给笔记设置 YAML `id`。Eagle 端插件需要单独安装，目前检查器声明支持 JPG、PNG，不是所有素材格式都会出现该面板。

## 10. 与 obEcraft 的图片流程联动

- EagleBridge 先完成外部素材入库或已有素材识别，再交给 obEcraft 选择原生嵌入或 Ecraft 嵌入。
- 改善同时启用时互相抢占粘贴、拖入事件，以及重复导入、意外生成本地附件的问题。
- Eagle 图片可以在 obEcraft 菜单中使用 EagleBridge 的素材操作。
- 为配合接入的插件提供生成图片存入 Eagle 的能力；实际入口由调用方提供。

联动需要双方使用支持该协作方式的版本。只使用 EagleBridge 时，原有拖入与上传能力仍可独立使用。

## 11. Mac、云盘和外接硬盘相关修复

- 补齐 macOS 路径、文件打开、Finder 定位和源文件复制等处理。
- 改善中文、空格、大小写及 Unicode 路径差异导致的素材定位问题。
- 针对 Mac 外接盘上的素材被误判为外部文件、拖入后在 Eagle 重复生成副本的问题，改进已有素材识别。
- 从未配置的 Eagle 库拖入时提示先配置，不再直接当作普通外部文件导入。
- 修复部分素材已经存入 Eagle、Obsidian 却没有返回链接的问题；新链接直接依据 Eagle 导入结果生成。
- 不再等待 iCloud 等环境中的目录新增事件来生成链接，避免原来的等待超时；同时去掉大素材库的目录监控负担及监控未清理问题。
- 多文件拖入时，某一项失败不再导致其他成功项的链接一起丢失；Canvas 导入异常也有明确提示。

上传需要 Eagle 4 或更新版本。云盘和外接盘素材仍需在当前电脑上可访问，纯云端占位文件不等于可预览。Mac 修复有针对性代码与模拟用例验证，尚未在真实 Mac 上完成全部场景实测，视频中不宜表述为“所有 Mac 问题已经解决”。

## 12. 预览、编辑与设置界面改进

- 修复编辑图片链接名称或尺寸时，普通点击意外弹出菜单的问题。
- 右键菜单整理为打开、复制、属性和删除等用途，减少查找成本。
- 修复视频上方出现裂图图标、重复文件名或不合适图片操作的问题，空名称的视频嵌入也可识别。
- 视频默认暂停，打开文档时不再自动播放；点击播放后才开始。
- 实时预览的视频右上角增加代码按钮，点击后显示并选中文字链接，方便改名称、尺寸或地址；光标离开后恢复预览。
- 改善视频分段读取和中断后的资源释放，减少大文件预览的额外负担。
- 设置面板按素材库、上传、预览、同步和维护等用途分组，精简说明文字、统一间距，并显示库路径可用状态。

## 发布前补充：主题与多 Obsidian 库共用

以下内容是在 `201b521` 之后继续完成的本次发布改进。

- Eagle 端链接面板改为透明底色，融入 Eagle 原生侧栏；文字和控件跟随 Eagle 自己的主题及主题切换，不再仅根据操作系统深浅模式判断。新增按钮、表单、长名称省略和窄面板滚动也已整理，原有链接操作保持不变。
- 同时打开多个 Obsidian 库时，同端口、同一个 Eagle 素材库共用一份预览服务，不再重复监听并弹出占用错误。
- 持有端口的窗口关闭或退出后，仍打开的窗口会自动接管；一般需要几秒，正在播放的媒体可能需要重新点击或刷新。
- 不同 Eagle 库不能共用同一端口；冲突会提示分别配置。关闭插件时会释放自己持有的监听及视频连接，其他窗口的共享服务不被误关。
- 所有同时打开的 Obsidian 库都应更新并重载插件，旧版服务无法参与本次共享识别。共享预览不等于跨 Obsidian 库合并引用索引或附件删除检查。
- Obsidian 插件版本更新为 **0.3.5**。

## 视频演示顺序建议

1. **先展示最终使用效果**：同一篇笔记和 Canvas 中，引用多个 Eagle 库里的图片、视频。
2. **展示如何收集**：外部素材拖入、选择目标库、控制文件类型，再演示已有文档附件的批量迁移。
3. **展示如何整理**：引用关系视图、标签同步、不同删除范围。
4. **展示如何流转**：从 Eagle 回到笔记，把笔记和附件导出给别人，再展示 obEcraft 联动。
5. **集中说明体验修复**：Mac 外接盘、iCloud 返回链接、视频不自动播放和代码编辑按钮。
6. **最后展示发布前调整**：Eagle 明暗主题、多 Obsidian 窗口共享预览，并提醒批量迁移和引用统计的范围。

可用开场口播：这次 EagleBridge 更新，主要补齐了白板、多 Eagle 库和附件整理这几条使用流程。除了把素材放进 Eagle，现在还能查看它被哪些笔记引用、批量迁移已有附件、连同笔记一起导出，并从 Eagle 回到笔记。之前大家反馈的 Mac 外接盘重复导入、素材入库却没有链接，以及视频预览和编辑问题，也做了针对性处理。
